# yq setup

## Important configuration paths

- `~/.bash_history`

## Installed state

- Installed version: `3.4.3`
- Executable path: `/usr/bin/yq`

## Current state

- `yq` is installed and available on `PATH`
- No shell-specific init or aliases are currently documented for `yq`

## History-backed setup notes

- `apt install yq`

## Caveats

- `yq` behaves like a standard CLI utility here; shell integration would only matter if you add aliases or functions later

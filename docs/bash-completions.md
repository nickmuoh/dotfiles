# Bash completions

The `bash-completions` package stows `~/.local/share/bash-completion/completions/keychain`.

Keep shell completion scripts in this package rather than in individual tool packages.

# AWS CLI

Command-line interface for Amazon Web Services.

## Install

Upstream repo: `https://github.com/aws/aws-cli`

Installed via snap (managed by `scripts/setup-tools.sh`):

```sh
sudo snap install aws-cli --classic
```

Binary: `/snap/bin/aws`

Snap must be available on the machine before running `scripts/setup-tools.sh`.

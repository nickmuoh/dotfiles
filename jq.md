# jq setup

## Important configuration paths

- `~/.bash_history`

## Installed state

- Installed version: `1.8.1`
- Executable path: `/usr/bin/jq`

## Current state

- `jq` is installed and available on `PATH`
- No shell-specific init or aliases are currently documented for `jq`

## History-backed setup notes

- `apt install jq`

## Caveats

- `jq` behaves like a standard CLI utility here; shell integration would only matter if you add aliases or functions later

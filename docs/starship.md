# Starship

The `starship` package manages `~/.config/starship.toml`. Interactive Bash initializes Starship only when `starship` is on `PATH`.

The configuration sets `command_timeout = 1500`, truncates directories to two segments, and customizes language, hostname, package, and OS symbols. It uses Nerd Font glyphs.

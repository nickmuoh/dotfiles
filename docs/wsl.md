# WSL configuration (/etc/wsl.conf)

The WSL configuration at `/etc/wsl.conf` enables `systemd`, sets the default user
to `nmuoh`, and mounts Windows drives with metadata support.

## Apply changes

Run `wsl --shutdown` from Windows, then restart the distribution.
